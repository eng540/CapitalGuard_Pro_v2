import DashboardLayout from "@/components/DashboardLayout";
import { SectionTitle, StatusPill } from "@/components/finance-ui";
import { previewRecommendations } from "@/lib/preview-data";
import { trpc } from "@/lib/trpc";
import { BellRing, CircleGauge, ListTree } from "lucide-react";

export default function Recommendations() { const query = trpc.capitalguard.recommendations.useQuery(); const rows = query.data?.length ? query.data : previewRecommendations; return <DashboardLayout><div dir="rtl" className="mx-auto max-w-[1380px]"><div className="mb-8 flex flex-col justify-between gap-4 lg:flex-row lg:items-end"><div><p className="mb-2 text-xs font-semibold uppercase tracking-[.2em] text-cyan-300">Live Signals</p><h1 className="text-3xl font-semibold">توصيات قابلة للتتبع، وليست رسائل عابرة.</h1><p className="mt-2 text-sm text-muted-foreground">IDs هرمية، حالة الأهداف، وسجل الأحداث مع تمييز AUTO وMANUAL.</p></div><div className="flex gap-3 text-xs text-muted-foreground"><span className="inline-flex items-center gap-1"><BellRing className="h-4 w-4 text-cyan-300"/>تنبيهات مرئية آمنة</span><span className="inline-flex items-center gap-1"><CircleGauge className="h-4 w-4 text-violet-300"/>لا تنفيذ تلقائي</span></div></div><div className="grid gap-4 lg:grid-cols-3">{rows.map((row: any) => <article key={row.ref ?? row.publicRef} className="rounded-3xl border border-white/8 bg-card/70 p-5"><div className="flex items-start justify-between"><div><p className="font-mono text-[11px] text-cyan-300">{row.ref ?? row.publicRef}</p><h2 className="mt-2 text-2xl font-semibold">{row.asset}</h2></div><StatusPill value={row.status}/></div><p className={row.side === "long" ? "mt-3 text-sm text-emerald-300" : "mt-3 text-sm text-rose-300"}>{row.side?.toUpperCase()} · {row.source ?? "CapitalGuard"}</p><div className="mt-5 grid grid-cols-2 gap-3"><Cell label="Entry" value={row.entry}/><Cell label="Stop" value={row.stop ?? row.stopLoss}/></div><div className="mt-3 rounded-2xl bg-white/[.035] p-4"><p className="text-[11px] text-muted-foreground">الأهداف</p><p className="mt-1 text-sm font-medium">{Array.isArray(row.targets) ? `${row.targets.length} targets` : row.targets}</p></div><div className="mt-5 border-t border-white/7 pt-4"><p className="mb-3 flex items-center gap-2 text-xs font-medium"><ListTree className="h-4 w-4 text-violet-300"/>Timeline</p><div className="space-y-2 text-xs text-muted-foreground"><p>• Initial signal <span className="text-cyan-200">AUTO</span></p><p>• Market order filled <span className="text-cyan-200">AUTO</span></p><p>• Review status available</p></div></div></article>)}</div></div></DashboardLayout>; }
function Cell({ label, value }: { label: string; value: unknown }) { return <div className="rounded-xl bg-white/[.04] p-3"><p className="text-[10px] text-muted-foreground">{label}</p><p className="mt-1 text-sm font-medium">{String(value ?? "—")}</p></div>; }
