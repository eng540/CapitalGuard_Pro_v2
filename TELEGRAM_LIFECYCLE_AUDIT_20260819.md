# تدقيق دورة حياة التوصيات والتتبع والإشعارات

**التاريخ:** 2026-08-19  
**النطاق:** Telegram UX، UserTrade tracking، Recommendation lifecycle، Publication Outbox، إشعارات الخاص والقنوات.

## الخلاصة التنفيذية

أثبتت الصور أن السلوك المرصود كان نتيجة ثلاثة أسباب مترابطة، وليس سببًا واحدًا. أولًا، بعض سجلات التتبع القديمة أُنشئت بقيمة `source_type=FORWARD` رغم وجود `source_recommendation_id`، ولذلك دخلت في عدادات Watchlist ولم تدخل في عداد Tracked Signals، كما ظهرت بطاقة المتداول بوسم Analyst Recommendation بدل Tracked Signal. ثانيًا، كانت صفقة المتداول المتتبعة كيانًا مستقلًا عن التوصية المصدر؛ مراقب السعر يعالج كل UserTrade بصورة مستقلة، ولم يكن إغلاق المحلل اليدوي ينعكس على الصفقة المتتبعة. ثالثًا، كانت إشعارات دورة UserTrade عامة ولا تحمل هوية السجل، بينما كانت ردود القناة المعتمدة على Outbox قابلة للتأخر بحكم التصميم eventual consistency.

## الأدلة المرئية المؤكدة

| الدليل | الملاحظة |
|---|---|
| المحفظة | `Watchlist (3)` مقابل `Tracked Signals (0)` رغم تنفيذ تتبع إشارات؛ هذا يثبت خلل تصنيف/ترحيل أو عدم اتساق المصدر، وليس أن التتبع لم يُنشأ بالضرورة. |
| بطاقة الصفقة المتتبعة | ظهور `Analyst Recommendation` مع `USR-.../T-...` يخلط بين التوصية المصدر وصفقة المتداول؛ البطاقة تحتاج وسم `Tracked Signal` ومرجع التوصية الأصلية. |
| رسالة Tracking Started | الرسالة كانت `Tracking Started: BTCUSDT` فقط، بلا UserTrade ref أو Recommendation ref أو Analyst/Channel. |
| إشعارات الخاص | `TP2 Hit #BTCUSDT` و`Final Target Hit! PnL...` لا تحدد أي UserTrade أو توصية. كما ظهر تكرار لبعض الرسائل، ما يصعّب التحقيق. |
| قناة المحلل | البطاقة تحتوي هوية توصية جيدة نسبيًا، لكن ردود TP/الإغلاق المختصرة لا تحمل Recommendation ID. |
| `/events` | ظهور `operation was cancelled` كان تجربة سيئة عند استخدام الأمر خارج سياق المحلل، رغم أن دليل الأوامر يعرضه ضمن أوامر المحلل. |
| الإغلاق والتحديث | الحالة الأساسية قد تتغير في قاعدة البيانات قبل وصول بطاقة القناة، لأن UPDATE/REPLY/CLOSE تمر عبر Publication Outbox والعامل الخلفي. هذا تأخير متوقع، لكنه يحتاج هوية ومراقبة وتسليم واضح. |

## المنطق الصحيح المعتمد

`Tracked Signals` ليس حالة بديلة عن `Watchlist` أو `Active`. إنه **تصنيف مصدر**: هل الصفقة أنشأها المتداول من توصية محلل؟ أما الحالة التشغيلية فهي `WATCHLIST` أو `PENDING_ACTIVATION` أو `ACTIVATED` أو `CLOSED`. لذلك من الصحيح أن تظهر الصفقة المتتبعة في Watchlist قبل تحقق الدخول، ثم في Active بعد التفعيل، مع بقائها ضمن Tracked Signals في كلا الوضعين.

عند إغلاق المحلل لتوصية مصدر، يجب أن تنتقل كل UserTrade مفتوحة مرتبطة بها إلى `CLOSED` مع تسجيل `SOURCE_CLOSED`، وتسجيل السبب `MANUAL` أو `AUTO`. هذا يمنع بقاء نسخة المتداول فعالة بعد انتهاء الأصل، ويجعل العلاقة بين المصدر والنسخة قابلة للتدقيق. أما التفعيل والـ TP الآليان فيمكن أن يعملا على UserTrade بصورة مستقلة، لكن يجب أن يحمل كل إشعار هوية UserTrade وهوية التوصية المصدر.

## الإصلاحات المنفذة على الفرع

تم إنشاء الفرع `feature/lifecycle-tracking-notifications-20260819` من `main`، وتضمن الإصلاحات التالية:

1. أصبح إنشاء التتبع يكتب `source_type=TRACKED_RECOMMENDATION`، ويربط `watched_channel_id` و`ChannelCatalog` عند توفر القناة، ويسجل حدث `TRACKING_STARTED`.
2. أضيفت migration `20251209_repair_tracked_trade_identity` لإصلاح السجلات التاريخية التي تحمل `source_recommendation_id` مع `FORWARD`، وربط قناة المتابعة عند إمكانية تحديدها.
3. أضيف `open_size_percent` إلى UserTrade عبر migration `20251208_add_user_trade_open_size`، مع backfill للقيم القديمة إلى 100% وإلى 0% للصفقات المغلقة.
4. أضيفت مزامنة المصدر إلى UserTrade المفتوحة عند الإغلاق والتعديل والإغلاق الجزئي، مع أحداث `SOURCE_CLOSED` و`SOURCE_PARTIAL` و`SOURCE_SL_UPDATED` و`SOURCE_TARGETS_UPDATED`.
5. أصبحت إشعارات الخاص تعرض UserTrade ref، والمرجع العام، ونوع المصدر، وRecommendation ref، وAnalyst/Channel، ووسم `AUTO` أو `MANUAL`.
6. أصبحت ردود دورة الحياة في القناة تضيف Recommendation ID ثابتًا بدل الاكتفاء بالزوج والسعر.
7. أصبحت بطاقات UserTrade المتتبعة تعرض مرجع التوصية الأصلية والمحلل والقناة، مع fallback صحيح للسجلات القديمة التي تحمل `FORWARD`.
8. أصبح `/events <id>` يعمل للمحلل على Recommendation events وللمتداول على UserTrade events، مع رسائل خطأ مفهومة بدل الرسالة العامة.
9. أصبحت رسالة تأكيد `/log` تعرض `USR-.../T-...` وGlobal Ref بدل رقم الصف الداخلي فقط.
10. أضيف توضيح في المحفظة يشرح أن Tracked Signals تصنيف مصدر مستقل عن Watchlist وActive.

## بوابات محلية أولية

| البوابة | النتيجة |
|---|---|
| الاختبارات الكاملة قبل تثبيت الحزمة | `101 passed, 1 skipped` |
| اختبارات التتبع ودورة الحياة بعد الإصلاح | `17 passed` في المجموعة المستهدفة |
| Compileall | ناجح |
| Flake8 الحرج للملفات المعدلة | ناجح: `F821,F401,E999` |
| Bandit | ناجح، مع تحذيرات تعليقات غير مؤثرة |
| Diff check | ناجح |

يوجد في المستودع تاريخيًا عدد من تحذيرات `F401` عند تشغيل Flake8 على كل `src/`، وهي خارج نطاق هذه الحزمة وموجودة قبلها؛ أما الملفات المعدلة في هذه الحزمة فاجتازت الفحص الحرج.

## ما يجب التحقق منه على Railway وTelegram

قبل الدمج النهائي يجب تطبيق migrations والتأكد من أن `Tracked Signals` يعرض السجلات التاريخية بعد backfill، ثم تنفيذ السيناريو التالي بحساب متداول واحد وتوصية محلل واحدة: تتبع توصية Pending، التأكد من ظهورها في Watchlist وTracked Signals، تفعيلها، تحقق TP1، تحقق TP2 أو إغلاق يدوي من المحلل، ثم التحقق من UserTrade وHistory والقناة والخاص. يجب حفظ `Recommendation ID` و`UserTrade ID` من كل خطوة ومقارنتها في الأحداث والرسائل.

كما يجب مراقبة Publication Outbox للتأكد من وجود صفوف `UPDATE` و`REPLY` و`CLOSE`، انتقالها إلى `SENT`، وعدم تراكم `RETRY/FAILED`. إذا تغيرت قاعدة البيانات ولم تصل البطاقة، فالتحقيق يكون من سجل Outbox والعامل، لا من زر Refresh فقط.

**قرار المرحلة:** الإصلاحات مناسبة للدمج بعد اجتياز الاختبارات وCI والتحقق التشغيلي. لا يبدأ الدفع أو Copy Trading قبل إثبات هذا السيناريو مع الاحتفاظ والاستقرار.
